# DRaFT: direct reward backprop.
name: draft

reward_multiplier: 0.1
lr: 5.0e-4
warmup_steps: 0
max_grad_norm: 2.0

# Generated samples per step, summed over GPUs (grad_accum derived from this).
total_batch_size: 256

num_steps: 100
